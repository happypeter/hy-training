const initState = [
        {title: 'test1',price:20},
        {title: 'test2',price:30},
        {title: 'test3',price:40},
    ]

const products = (state = initState, action) => {
  return state
}

export default products
