const initState = [
        {productName: 'name1',price:20},
        {productName: 'name2',price:30},
        {productName: 'name3',price:40},
    ]

const products = (state = initState, action) => {
  return state
}

export default products
